已改进设置页震动指示器样式和数值格式，使其更接近原型页设计：

**HTML 改进：**
- 背景从 `rgba(255,255,255,0.04)` 改为 `#161625`（深色卡片）
- 边框从 `rgba(255,255,255,0.08)` 改为 `#282840`（更明显）
- 标题样式：大写、字间距、dim 颜色 `#7a7a96`
- 进度条高度从 `8px` 改为 `14px`，圆角 `7px`
- 背景轨道从 `rgba(255,255,255,0.1)` 改为 `#1a1a2e`（深色，有对比）
- 强震填充从 `var(--primary)`（琥珀金）改为 `linear-gradient(90deg, #ff6644, #ff9944)`（原型页暖橙渐变）
- 弱震填充从 `rgba(154,200,226,0.7)` 改为 `linear-gradient(90deg, #44aaff, #44ccff)`（原型页冷蓝渐变）
- 数值标签从百分比 `0%` 改为 `0.00`（原型页风格）

**JS 改进：**
- `sStrongV.textContent` 从 `(strong * 100).toFixed(0) + '%'` 改为 `strong.toFixed(2)`
- `sWeakV.textContent` 同理

这样与原型页 `gamepad-audio-rumble.html` 的视觉风格一致。