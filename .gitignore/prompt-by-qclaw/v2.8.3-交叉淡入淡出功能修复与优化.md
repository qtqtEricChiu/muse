                text: original,           // 涓绘樉绀烘枃鏈紙鍘熸枃锛?                original: original,       // 鍘熸枃
                translation: translation, // 缈昏瘧
                isBilingual: true         // 鏍囪涓哄弻璇
            });
            i++; // 璺宠繃宸插悎骞剁殑涓嬩竴琛?        } else {
            // 鍗曡姝岃瘝锛屼繚鎸佸師鏈夌粨鏋?            result.push({
                time: current.time,
                text: current.text,
                original: current.text,
                translation: null,
                isBilingual: false
            });
        }
    }
    
    return result;
}
```

#### 姝ラ2锛氫慨鏀?`loadLrc()` 娓叉煋鍙岃姝岃瘝DOM

**鏂囦欢**: `app.js`  
**浣嶇疆**: 绗?1316-1323 琛?
**淇敼鍚?*:
```javascript
    parsedLyrics.forEach((l) => {
        const d = document.createElement('div');
        d.className = 'lrc-line';
        d.dataset.time = l.time; // 馃殌 v2.8.3: 瀛樺偍鏃堕棿鎴崇敤浜庡悓姝ュ尮閰?        
        if (l.isBilingual) {
            // 鍙岃琛岋細鍘熸枃 + 缈昏瘧
            d.innerHTML = `
                <span class="lrc-original">${escapeHtml(l.original)}</span>
                <span class="lrc-translation">${escapeHtml(l.translation)}</span>
            `;
            d.classList.add('bilingual');
        } else {
            d.textContent = l.text;
        }
        
        d.onclick = () => { audio.currentTime = l.time + lyricsOffset; syncLyrics(true); };
        el.lrcView.appendChild(d);
    });
```

**鏂板杈呭姪鍑芥暟**锛堟斁鍦?`parseLyricText` 闄勮繎锛夛細
```javascript
// 馃殌 v2.8.3: HTML杞箟闃叉XSS
function escapeHtml(text) {
    const div = document.createElement('div');
    div.textContent = text;
    return div.innerHTML;
}
```

#### 姝ラ3锛氫慨鏀?`syncLyrics()` 鏀寔鍙岃鏄剧ず

**鏂囦欢**: `app.js`  
**浣嶇疆**: 绗?1342-1368 琛?
**淇敼鍚?*:
```javascript
const syncLyrics = (force = false) => {
    if(!parsedLyrics.length) return;
    const cur = audio.currentTime - lyricsOffset;
    let activeIdx = -1;
    for (let i = 0; i < parsedLyrics.length; i++) { 
        if (cur >= parsedLyrics[i].time - 0.2) activeIdx = i; 
        else break; 
    }

    // 涓荤晫闈㈡瓕璇嶉潰鏉块珮浜?    if (el.lrcPanel.style.display !== 'none') {
        const lines = el.lrcView.querySelectorAll('.lrc-line');
        lines.forEach((line, i) => {
            if (i === activeIdx) {
                if (!line.classList.contains('active')) {
                    line.classList.add('active');
                    if (!isUserScrollingLyrics || force) {
                        const offset = line.offsetTop - el.lrcView.offsetTop - (el.lrcView.clientHeight / 2) + (line.clientHeight / 2);
                        el.lrcView.scrollTo({ top: offset, behavior: 'smooth' });
                    }
                }
            } else line.classList.remove('active');
        });
    }

    // 馃殌 v2.8.3: 娌夋蹈妯″紡鍙岃姝岃瘝鏄剧ず浼樺寲
    if (activeIdx !== -1 && !el.immLrcCenter.classList.contains('hidden')) {
        const currentLrc = parsedLyrics[activeIdx];
        
        if (currentLrc.isBilingual) {
            // 鍙岃琛岋細鍘熸枃澶у瓧楂樹寒 + 缈昏瘧琛岃窡闅忔樉绀?            const displayText = `${currentLrc.original}\n${currentLrc.translation}`;
            if(el.immCurrLine.textContent !== displayText) {
                el.immCurrLine.style.opacity = 0;
                setTimeout(() => {
                    el.immCurrLine.innerHTML = `
                        <span class="imm-original">${escapeHtml(currentLrc.original)}</span>
                        <span class="imm-translation">${escapeHtml(currentLrc.translation)}</span>
                    `;
                    el.immCurrLine.style.opacity = 1;
                }, 200);
            }
            
            // 涓嬩竴琛岋細鎵惧埌涓嬩竴涓笉鍚屾椂闂存埑鐨勬瓕璇?            let nextIdx = activeIdx + 1;
            while (nextIdx < parsedLyrics.length && 
                   Math.abs(parsedLyrics[nextIdx].time - currentLrc.time) < 0.02) {
                nextIdx++;
            }
            const nextLrc = nextIdx < parsedLyrics.length ? parsedLyrics[nextIdx] : null;
            const nextTxt = nextLrc ? nextLrc.text : '';
            
            if(el.immNextLine.textContent !== nextTxt) {
                el.immNextLine.style.opacity = 0;
                setTimeout(() => { 
                    el.immNextLine.textContent = nextTxt; 
                    el.immNextLine.style.opacity = 1; 
                }, 200);
            }
        } else {
            // 鍗曡琛岋細淇濇寔鍘熸湁閫昏緫
            const curTxt = currentLrc.text;
            const nextTxt = activeIdx+1 < parsedLyrics.length ? parsedLyrics[activeIdx+1].text : '';
            if(el.immCurrLine.textContent !== curTxt) { 
                el.immCurrLine.innerHTML = escapeHtml(curTxt); 
                el.immCurrLine.style.opacity=0; 
                setTimeout(()=>{ el.immCurrLine.style.opacity=1; }, 200); 
            }
            if(el.immNextLine.textContent !== nextTxt) { 
                el.immNextLine.textContent = nextTxt; 
                el.immNextLine.style.opacity=0; 
                setTimeout(()=>{ el.immNextLine.style.opacity=1; }, 200); 
            }
        }
    }
};
```

#### 姝ラ4锛氭柊澧炴瓕璇嶉潰鏉緾SS鏍峰紡

**鏂囦欢**: `css/base-layout.css`  
**浣嶇疆**: 鍦ㄧ 203 琛屼箣鍚庤拷鍔?
```css
/* 馃殌 v2.8.3: 鍙岃姝岃瘝鏄剧ず浼樺寲 */
.lrc-line.bilingual {
    display: flex;
    flex-direction: column;
    gap: 4px;
    padding: 8px 0;
}

.lrc-line .lrc-original {
    display: block;
    font-size: inherit;
    color: inherit;
}

.lrc-line .lrc-translation {
    display: block;
    font-size: calc(var(--lrc-font-size) * 0.92);
    color: rgba(255,255,255,0.65);
    font-weight: 500;
    opacity: 0.9;
    transition: all 0.6s cubic-bezier(0.2, 0.8, 0.2, 1);
}

/* 婵€娲荤姸鎬侊細鍘熸枃楂樹寒锛岀炕璇戣窡闅忔彁浜?*/
.lrc-line.active .lrc-translation {
    font-size: calc(var(--lrc-font-size) * 1.15);
    color: rgba(255,255,255,0.88);
    opacity: 1;
    font-weight: 600;
}

/* 闈炴縺娲荤姸鎬侊細缈昏瘧娣″寲 */
.lrc-line:not(.active) .lrc-translation {
    opacity: 0.55;
    font-size: calc(var(--lrc-font-size) * 0.85);
}

/* 绉婚櫎鏃х殑鐩搁偦鍏勫紵閫夋嫨鍣紝閬垮厤璇珮浜炕璇戣 */
.lrc-line.active + .lrc-line:not(.bilingual) {
    filter: blur(0px) !important;
    opacity: 0.75;
    transform: scale(0.98);
}
```

#### 姝ラ5锛氫慨鏀规矇娴告ā寮廋SS

**鏂囦欢**: `css/immersive.css`  
**浣嶇疆**: 鍦ㄧ 54 琛屼箣鍚庤拷鍔?
```css
/* 馃殌 v2.8.3: 娌夋蹈妯″紡鍙岃姝岃瘝 */
.imm-subtitle-line .imm-original {
    display: block;
    font-size: inherit;
    font-weight: inherit;
    color: inherit;
}

.imm-subtitle-line .imm-translation {
    display: block;
    font-size: clamp(22px, 3vw, 36px);
    font-weight: 600;
    color: rgba(255,255,255,0.85);
    margin-top: 8px;
    opacity: 0.95;
    text-shadow: 0 2px 10px rgba(0,0,0,0.6), 0 0 20px var(--primary-glow);
    transition: all 0.4s var(--curve-spring);
}

/* 鐢讳腑鐢绘ā寮忎笅鐨勫弻璇瓕璇嶉€傞厤 */
@media (max-width: 480px) {
    .imm-subtitle-line .imm-translation {
        font-size: clamp(16px, 2vw, 24px);
        margin-top: 4px;
    }
}
```

#### 姝ラ6锛氫慨鏀?`getLyricAtTime()` 鏀寔鍙岃

**鏂囦欢**: `app.js`  
**浣嶇疆**: 绗?1373-1383 琛?
**淇敼鍚?*:
```javascript
function getLyricAtTime(time) {
    if (!parsedLyrics.length) return null;
    const adjustedTime = time - lyricsOffset;
    let best = null;
    for (let i = 0; i < parsedLyrics.length; i++) {
        if (parsedLyrics[i].time <= adjustedTime) best = parsedLyrics[i];
        else break;
    }
    
    // 馃殌 v2.8.3: 鍙岃姝岃瘝鏄剧ず鍘熸枃+缈昏瘧
    if (best && best.isBilingual) {
        return {
            ...best,
            text: `${best.original} | ${best.translation}` // 杩涘害鏉￠瑙堟樉绀哄悎骞舵枃鏈?        };
    }
    return best;
}
```

#### 姝ラ7锛氫慨鏀圭敾涓敾姝岃瘝鏄剧ず

**鏂囦欢**: `js/pip.js`锛堟垨 `app.js` 涓敾涓敾鐩稿叧浠ｇ爜锛?
濡傛灉鐢讳腑鐢荤獥鍙ｄ腑鏈夌嫭绔嬬殑姝岃瘝鏄剧ず閫昏緫锛岄渶瑕佸悓姝ヤ慨鏀逛互鏀寔 `.isBilingual` 鏍囪銆傛牳蹇冮€昏緫涓庢矇娴告ā寮忕被浼硷細
- 妫€娴嬪埌 `isBilingual` 鏃讹紝鍘熸枃姝ｅ父鏄剧ず锛岀炕璇戣浠ユ洿楂樹笉閫忔槑搴︺€佹洿澶у瓧鍙锋樉绀?- 鏈娴嬪埌鍒欎繚鎸佸師鏈夊崟琛岄€昏緫

---

### 馃И 娴嬭瘯楠岃瘉

1. **鍙岃LRC鍔犺浇娴嬭瘯**锛?   - 鍔犺浇 `LEMONADE - aespa.lrc`
   - **棰勬湡**锛氬悓鏃堕棿鎴崇殑涓よ姝岃瘝鍚堝苟涓轰竴缁勶紝鍘熸枃鍦ㄤ笂锛岀炕璇戝湪涓?   - 妫€鏌?`parsedLyrics` 鏁扮粍锛岀‘璁?`isBilingual: true` 涓斿寘鍚?`original` / `translation`

2. **涓荤晫闈㈡瓕璇嶉潰鏉挎祴璇?*锛?   - 鎾斁鑷冲弻璇瓕璇嶆椂闂寸偣
   - **棰勬湡**锛氬師鏂囨甯搁珮浜紝缈昏瘧琛屼互杈冨ぇ瀛楀彿銆佽緝楂樹笉閫忔槑搴︽樉绀?   - 闈炴縺娲昏锛氱炕璇戝簲娣″寲锛坥pacity 0.55锛?
3. **娌夋蹈妯″紡娴嬭瘯**锛?   - 鍒囨崲鑷虫矇娴告ā寮?   - **棰勬湡**锛歚immCurrLine` 鏄剧ず鍘熸枃锛堝ぇ瀛楋級锛岀炕璇戣绱ч殢鍏跺悗锛堢◢灏忎絾娓呮櫚锛?   - `immNextLine` 鏄剧ず涓嬩竴缁勬瓕璇嶏紙闈炲綋鍓嶇炕璇戣锛?
4. **杩涘害鏉℃偓鍋滈瑙堟祴璇?*锛?   - 榧犳爣鎮仠鍦ㄨ繘搴︽潯涓?   - **棰勬湡**锛氭彁绀烘鏄剧ず `"馃帳 鍘熸枃 | 缈昏瘧 [鏃堕棿]"`

5. **鍗曡LRC鍏煎鎬ф祴璇?*锛?   - 鍔犺浇鏅€氬崟璇璍RC
   - **棰勬湡**锛氳涓轰笌淇敼鍓嶅畬鍏ㄤ竴鑷达紝鏃犲洖褰?
---

### 馃搵 淇敼鏂囦欢娓呭崟

| 鏂囦欢 | 淇敼鍐呭 | 琛屽彿鑼冨洿 |
|------|---------|---------|
| `app.js` | 閲嶅啓 `parseLyricText()` 鏀寔鍙岃妫€娴嬪悎骞?| 绗?1260-1279 琛?|
| `app.js` | 鏂板 `escapeHtml()` 杈呭姪鍑芥暟 | ~绗?1255 琛?|
| `app.js` | 淇敼 `loadLrc()` 娓叉煋鍙岃DOM | 绗?1316-1323 琛?|
| `app.js` | 淇敼 `syncLyrics()` 娌夋蹈妯″紡鍙岃閫昏緫 | 绗?1342-1368 琛?|
| `app.js` | 淇敼 `getLyricAtTime()` 杩斿洖鍚堝苟鏂囨湰 | 绗?1373-1383 琛?|
| `css/base-layout.css` | 鏂板 `.lrc-line.bilingual` 绛夋牱寮?| 绗?203 琛屽悗 |
| `css/immersive.css` | 鏂板 `.imm-translation` 鏍峰紡 | 绗?54 琛屽悗 |

---

## 馃敡 闂6锛氶煶杞ㄤ氦鍙夋贰鍏ユ贰鍑猴紙Crossfade锛夊姛鑳戒慨澶嶄笌浼樺寲锛坴2.8.3锛?
> **鏂板鏃ユ湡**: 2026-06-02  
> **鐩爣鐗堟湰**: v2.8.3  
> **浠诲姟**: 妫€鏌ュ綋鍓嶄氦鍙夋贰鍏ユ贰鍑哄姛鑳藉湪 Chrome 鏈€鏂扮増涓殑鍏煎鎬э紝淇宸茬煡闂骞朵紭鍖?
---

### 馃搷 闇€姹傛弿杩?
**褰撳墠瀹炵幇**锛?- `setupCrossfade()` 鐩戝惉 `timeupdate` 浜嬩欢锛屽湪姝屾洸鍓╀綑鏃堕棿 鈮?`crossfadeDuration` 鏃惰Е鍙?`triggerFadeOut()`
- `triggerFadeOut()` 浣跨敤 `setInterval` 閫愭闄嶄綆闊抽噺鑷?0锛岀劧鍚庤皟鐢?`goNext()` 鍒囨瓕锛屽啀璋冪敤 `triggerFadeIn()`
- `triggerFadeIn()` 绛夊緟 `playing` 浜嬩欢鍚庯紝浣跨敤 `setInterval` 閫愭鎭㈠闊抽噺

**宸茬煡闂**锛?1. **Chrome 鏈€鏂扮増鍏煎鎬?*锛欳hrome 浠?v66 寮€濮嬮檺鍒?`AudioContext` 鑷姩鎾斁锛屼絾 `HTMLAudioElement` 鐨?`volume` 灞炴€т笉鍙楁闄愬埗銆傜劧鑰岋紝`timeupdate` 浜嬩欢鍦ㄨ儗鏅爣绛鹃〉涓殑瑙﹀彂棰戠巼浼氶檷浣庯紝瀵艰嚧浜ゅ弶娣″叆娣″嚭鏃舵満涓嶅噯纭€?2. **`timeupdate` 绮惧害闂**锛歚timeupdate` 浜嬩欢鐨勮Е鍙戦鐜囩害涓?250ms 涓€娆★紝涓嶅绮剧‘銆傚綋 `crossfadeDuration` 杈冪煭锛堝 1 绉掞級鏃讹紝鍙兘閿欒繃鏈€浣宠Е鍙戞椂鏈恒€?3. **`goNext()` 璋冪敤鏃舵満**锛氬綋鍓嶅湪 `fadeOut` 瀹屾垚鍚庢墠璋冪敤 `goNext()`锛岃繖鎰忓懗鐫€鍦ㄦ贰鍑烘湡闂寸敤鎴峰惉鍒扮殑浠嶆槸褰撳墠姝屾洸鐨勯潤闊崇増鏈紝鑰屼笉鏄笅涓€棣栨瓕鐨勬贰鍏ャ€傜湡姝ｇ殑浜ゅ弶娣″叆娣″嚭搴旇鏄?*涓ら姝屾洸鍚屾椂鎾斁锛屼竴棣栨贰鍑猴紝涓€棣栨贰鍏?*銆?4. **`isFading` 閿佺殑缂洪櫡**锛氬鏋滅敤鎴峰湪浜ゅ弶娣″叆娣″嚭鏈熼棿鎵嬪姩鍒囨瓕锛宍isFading` 閿佸彲鑳芥棤娉曟纭噴鏀撅紝瀵艰嚧鍚庣画鎾斁寮傚父銆?5. **`triggerFadeIn` 鐨?`playing` 浜嬩欢鐩戝惉**锛氫娇鐢?`{ once: false }` 鏄敊璇殑锛屽簲璇ヤ娇鐢?`{ once: true }`銆?
---

### 馃幆 浠ｇ爜鍒嗘瀽

#### 褰撳墠浜ゅ弶娣″叆娣″嚭瀹炵幇

**鏂囦欢**: `app.js` 绗?1506-1600 琛?
```javascript
function setupCrossfade() {
    audio.addEventListener('timeupdate', () => {
        if (!crossfadeEnabled || isRepeatOne || playlist.length < 2) return;
        const remaining = audio.duration - audio.currentTime;
        if (remaining <= crossfadeDuration && !isFading && remaining > 0.5) {
            isFading = true;
            triggerFadeOut();
        }
    });
}

function triggerFadeOut() {
    const userVolume = parseFloat(el.volSlider.value);
    const step = 0.05;
    const stepsCount = userVolume / step;
    const intervalTime = stepsCount > 0 ? (crossfadeDuration * 1000) / stepsCount : 100;
    
    const fadeOutInterval = setInterval(() => {
        if (audio.volume > step) {
            audio.volume = Math.max(0, audio.volume - step);
        } else {
            clearInterval(fadeOutInterval);
            audio.volume = 0;
            goNext(); // 鍒囨瓕
            triggerFadeIn(userVolume); // 娣″叆
        }
    }, intervalTime);
}

function triggerFadeIn(targetVolume) {
    audio.volume = 0;
    const step = 0.05;
    const fadeInDuration = 1.5;
    const stepsCount = targetVolume / step;
    const intervalTime = stepsCount > 0 ? (fadeInDuration * 1000) / stepsCount : 100;
    
    let fadeInInterval = null;
    
    const onPlaying = () => {
        audio.removeEventListener('playing', onPlaying);
        fadeInInterval = setInterval(() => {
            if (audio.volume < targetVolume - step) {
                audio.volume = Math.min(targetVolume, audio.volume + step);
            } else {
                clearInterval(fadeInInterval);
                audio.volume = targetVolume;
                isFading = false;
            }
        }, intervalTime);
    };
    
    if (!audio.paused && audio.currentTime > 0 && audio.readyState >= 2) {
        onPlaying();
    } else {
        audio.addEventListener('playing', onPlaying, { once: false }); // 鉂?閿欒锛氬簲涓?{ once: true }
        setTimeout(() => {
            audio.removeEventListener('playing', onPlaying);
            if (isFading && audio.volume === 0) {
                audio.volume = targetVolume;
                isFading = false;
            }
        }, 5000);
    }
}
```

---

### 馃洜锔?淇鏂规

#### 姝ラ1锛氫娇鐢?`requestAnimationFrame` 鏇夸唬 `setInterval` 瀹炵幇骞虫粦娣″叆娣″嚭

**鏂囦欢**: `app.js`  
**浣嶇疆**: 绗?1506-1600 琛?
**淇敼鍚?*:
```javascript
// 馃殌 v2.8.3: 浜ゅ弶娣″叆娣″嚭閲嶆瀯 鈥?浣跨敤 rAF 鏇夸唬 setInterval锛屾敮鎸佺湡姝ｇ殑鍙岃建浜ゅ弶
let crossfadeRafId = null;
let crossfadeStartTime = null;
let crossfadeFromVolume = 1;
let crossfadeTargetVolume = 1;
let crossfadeCallback = null;

function setupCrossfade() {
    // 浣跨敤鏇寸簿纭殑杩涘害妫€娴嬶細缁撳悎 timeupdate 鍜?requestAnimationFrame
    audio.addEventListener('timeupdate', () => {
        if (!crossfadeEnabled || isRepeatOne || playlist.length < 2) return;
        
        const remaining = audio.duration - audio.currentTime;
        // 鎻愬墠 crossfadeDuration 绉掑紑濮嬫贰鍑猴紝缁欐贰鍏ョ暀鍑烘椂闂?        if (remaining <= crossfadeDuration && !isFading && remaining > 0.5) {
            isFading = true;
            triggerCrossfade();
        }
    });
}

// 馃殌 v2.8.3: 鐪熸鐨勪氦鍙夋贰鍏ユ贰鍑?鈥?鍙岃建鍚屾椂鎾斁
function triggerCrossfade() {
    const userVolume = parseFloat(el.volSlider.value);
    const nextIndex = isShuffle 
        ? Math.floor(Math.random() * playlist.length) 
        : (currentIndex + 1) % playlist.length;
    
    // 鍒涘缓绗簩涓煶棰戝厓绱犵敤浜庨鍔犺浇涓嬩竴棣?    const nextAudio = new Audio();
    nextAudio.src = playlist[nextIndex].url;
    nextAudio.volume = 0; // 鍒濆闈欓煶
    
    // 棰勫姞杞戒笅涓€棣?    nextAudio.addEventListener('canplaythrough', () => {
        // 寮€濮嬩氦鍙夋贰鍏ユ贰鍑?        const startTime = performance.now();
        const duration = crossfadeDuration * 1000;
        
        const fadeStep = (timestamp) => {
            const elapsed = timestamp - startTime;
            const progress = Math.min(elapsed / duration, 1);
            
            // 褰撳墠姝屾洸娣″嚭
            audio.volume = userVolume * (1 - progress);
            // 涓嬩竴棣栨贰鍏?            nextAudio.volume = userVolume * progress;
            
            if (progress < 1) {
                crossfadeRafId = requestAnimationFrame(fadeStep);
            } else {
                // 浜ゅ弶娣″叆娣″嚭瀹屾垚
                finishCrossfade(nextAudio, nextIndex, userVolume);
            }
        };
        
        // 鍚屾椂鎾斁涓ら姝屾洸
        nextAudio.play().catch(e => {
            console.warn('棰勫姞杞芥挱鏀惧け璐?', e);
            // 鍥為€€鍒版棫鐗堥€昏緫
            triggerFadeOutLegacy(userVolume);
        });
        
        crossfadeRafId = requestAnimationFrame(fadeStep);
    }, { once: true });
    
    // 鍏滃簳锛氬鏋滈鍔犺浇澶辫触锛屼娇鐢ㄦ棫鐗堥€昏緫
    nextAudio.addEventListener('error', () => {
        console.warn('涓嬩竴棣栭鍔犺浇澶辫触锛屼娇鐢ㄦ棫鐗堟贰鍏ユ贰鍑?);
        triggerFadeOutLegacy(userVolume);
    }, { once: true });
}

// 瀹屾垚浜ゅ弶娣″叆娣″嚭
function finishCrossfade(nextAudio, nextIndex, userVolume) {
    cancelAnimationFrame(crossfadeRafId);
    crossfadeRafId = null;
    
    // 鍋滄褰撳墠闊抽
    audio.pause();
    audio.src = '';
    
    // 鍒囨崲鍒版柊闊抽
    audio.src = nextAudio.src;
    audio.volume = userVolume;
    
    // 鏇存柊鎾斁鐘舵€?    currentIndex = nextIndex;
    const song = playlist[currentIndex];
    
    // 鍚屾UI
    el.mainTitle.textContent = el.immTitle.textContent = song.title;
    el.mainArtist.textContent = el.immArtist.textContent = song.artist;
    document.title = `${song.title} - ${song.artist}`;
    
    // 搴旂敤鎾斁閫熷害
    audio.playbackRate = playbackRate;
    audio.preservesPitch = preservesPitch;
    
    // 鎾斁鏂伴煶棰?    audio.play().then(() => {
        setPlayState(true);
        isFading = false;
    }).catch(e => {
        console.error('浜ゅ弶娣″叆娣″嚭鍚庢挱鏀惧け璐?', e);
        isFading = false;
    });
    
    // 娓呯悊
    nextAudio.pause();
    nextAudio.src = '';
}

// 鏃х増娣″叆娣″嚭锛堥檷绾ф柟妗堬級
function triggerFadeOutLegacy(userVolume) {
    const step = 0.05;
    const stepsCount = userVolume / step;
    const intervalTime = stepsCount > 0 ? (crossfadeDuration * 1000) / stepsCount : 100;
    
    const fadeOutInterval = setInterval(() => {
        if (audio.volume > step) {
            audio.volume = Math.max(0, audio.volume - step);
        } else {
            clearInterval(fadeOutInterval);
            audio.volume = 0;
            goNext();
            triggerFadeInLegacy(userVolume);
        }
    }, intervalTime);
}

function triggerFadeInLegacy(targetVolume) {
    audio.volume = 0;
    const step = 0.05;
    const fadeInDuration = 1.5;
    const stepsCount = targetVolume / step;
    const intervalTime = stepsCount > 0 ? (fadeInDuration * 1000) / stepsCount : 100;
    
    let fadeInInterval = null;
    
    const onPlaying = () => {
        audio.removeEventListener('playing', onPlaying);
        
        fadeInInterval = setInterval(() => {
            if (audio.volume < targetVolume - step) {
                audio.volume = Math.min(targetVolume, audio.volume + step);
            } else {
                clearInterval(fadeInInterval);
                audio.volume = targetVolume;
                isFading = false;
            }
        }, intervalTime);
    };
    
    if (!audio.paused && audio.currentTime > 0 && audio.readyState >= 2) {
        onPlaying();
    } else {
        // 鉁?v2.8.3 淇锛氫娇鐢?{ once: true }
        audio.addEventListener('playing', onPlaying, { once: true });
        setTimeout(() => {
            audio.removeEventListener('playing', onPlaying);
            if (isFading && audio.volume === 0) {
                audio.volume = targetVolume;
                isFading = false;
            }
        }, 5000);
    }
}
```

#### 姝ラ2锛氫慨澶?`playAudio()` 涓殑 `isFading` 閿?
**鏂囦欢**: `app.js`  
**浣嶇疆**: 鍦?`playAudio()` 鍑芥暟寮€澶?
**淇敼鍚?*:
```javascript
const playAudio = async (idx) => {
    if (!playlist[idx]) return;
    
    // 馃殌 v2.8.3: 鎵嬪姩鍒囨瓕鏃跺己鍒跺彇娑堟鍦ㄨ繘琛岀殑浜ゅ弶娣″叆娣″嚭
    if (crossfadeRafId) {
        cancelAnimationFrame(crossfadeRafId);
        crossfadeRafId = null;
    }
    isFading = false;
    audio.volume = parseFloat(el.volSlider.value);
    
    // ... 鍘熸湁閫昏緫 ...
};
```

#### 姝ラ3锛氭坊鍔犱氦鍙夋贰鍏ユ贰鍑虹姸鎬佹寚绀哄櫒

**鏂囦欢**: `css/components.css`  
**浣嶇疆**: 鏂板

```css
/* 馃殌 v2.8.3: 浜ゅ弶娣″叆娣″嚭鐘舵€佹寚绀?*/
.crossfade-indicator {
    position: fixed;
    bottom: 80px;
    right: 20px;
    background: rgba(0,0,0,0.6);
    backdrop-filter: blur(10px);
    padding: 8px 16px;
    border-radius: 20px;
    font-size: 12px;
    color: var(--primary);
    opacity: 0;
    transition: opacity 0.3s;
    pointer-events: none;
    z-index: 100;
}
.crossfade-indicator.active {
    opacity: 1;
}
```

#### 姝ラ4锛氫紭鍖?`timeupdate` 妫€娴嬬簿搴?
**鏂囦欢**: `app.js`  
**浣嶇疆**: `setupCrossfade()`

**淇敼鍚?*:
```javascript
function setupCrossfade() {
    // 浣跨敤 requestAnimationFrame 杈呭姪妫€娴嬶紝鎻愰珮绮惧害
    let lastCheckTime = 0;
    
    const checkCrossfade = () => {
        if (!crossfadeEnabled || isRepeatOne || playlist.length < 2 || isFading) {
            requestAnimationFrame(checkCrossfade);
            return;
        }
        
        const remaining = audio.duration - audio.currentTime;
        
        // 浣跨敤 performance.now() 杩涜鏇寸簿纭殑鏃堕棿鍒ゆ柇
        const now = performance.now();
        if (now - lastCheckTime > 100) { // 姣?100ms 妫€鏌ヤ竴娆?            lastCheckTime = now;
            
            if (remaining <= crossfadeDuration && remaining > 0.5) {
                isFading = true;
                triggerCrossfade();
            }
        }
        
        requestAnimationFrame(checkCrossfade);
    };
    
    // 鍚姩妫€娴嬪惊鐜?    requestAnimationFrame(checkCrossfade);
    
    // 淇濈暀 timeupdate 浣滀负鍏滃簳
    audio.addEventListener('timeupdate', () => {
        if (!crossfadeEnabled || isRepeatOne || playlist.length < 2) return;
        const remaining = audio.duration - audio.currentTime;
        if (remaining <= crossfadeDuration && !isFading && remaining > 0.5) {
            isFading = true;
            triggerCrossfade();
        }
    });
}
```

---

### 馃И 娴嬭瘯楠岃瘉

1. **Chrome 鏈€鏂扮増鍏煎鎬ф祴璇?*锛?   - 鍦?Chrome 鏈€鏂扮増涓紑鍚氦鍙夋贰鍏ユ贰鍑?   - **棰勬湡**锛氭瓕鏇插湪鍓╀綑 `crossfadeDuration` 绉掓椂骞虫粦杩囨浮鍒颁笅涓€棣?   - 妫€鏌ユ帶鍒跺彴鏄惁鏈?`AudioContext` 鑷姩鎾斁闄愬埗璀﹀憡

2. **鍙岃建浜ゅ弶娣″叆娣″嚭娴嬭瘯**锛?   - 鎾斁涓€棣栨瓕鏇诧紝绛夊緟鎺ヨ繎缁撳熬
   - **棰勬湡**锛氬綋鍓嶆瓕鏇查煶閲忛€愭笎闄嶄綆锛屼笅涓€棣栨瓕鏇查煶閲忛€愭笎鍗囬珮锛屼袱棣栨瓕鏇叉湁鐭殏鐨勯噸鍙?   - 妫€鏌ユ槸鍚︽湁鐖嗛煶鎴栭潤闊?
3. **鎵嬪姩鍒囨瓕娴嬭瘯**锛?   - 鍦ㄤ氦鍙夋贰鍏ユ贰鍑烘湡闂存墜鍔ㄧ偣鍑讳笅涓€棣?   - **棰勬湡**锛氫氦鍙夋贰鍏ユ贰鍑虹珛鍗充腑鏂紝姝ｅ父鎾斁涓嬩竴棣?
4. **`isFading` 閿佹祴璇?*锛?   - 蹇€熻繛缁偣鍑讳笅涓€棣?   - **棰勬湡**锛氭瘡娆￠兘鑳芥甯告挱鏀撅紝涓嶄細鍑虹幇姘镐箙闈欓煶

5. **闄嶇骇鏂规娴嬭瘯**锛?   - 妯℃嫙缃戠粶閿欒锛屼娇棰勫姞杞藉け璐?   - **棰勬湡**锛氳嚜鍔ㄥ洖閫€鍒版棫鐗堟贰鍏ユ贰鍑洪€昏緫

---

### 馃搵 淇敼鏂囦欢娓呭崟

| 鏂囦欢 | 淇敼鍐呭 | 琛屽彿鑼冨洿 |
|------|---------|---------|
| `app.js` | 鏂板 `crossfadeRafId` 绛夊彉閲?| ~绗?78 琛?|
| `app.js` | 閲嶅啓 `setupCrossfade()` | 绗?1506-1518 琛?|
| `app.js` | 鏂板 `triggerCrossfade()` | ~绗?1519 琛?|
| `app.js` | 鏂板 `finishCrossfade()` | ~绗?1560 琛?|
| `app.js` | 淇濈暀 `triggerFadeOutLegacy()` | 绗?1521-1543 琛?|
| `app.js` | 淇濈暀 `triggerFadeInLegacy()` | 绗?1545-1600 琛?|
| `app.js` | 淇敼 `playAudio()` 鍙栨秷浜ゅ弶娣″叆娣″嚭 | ~绗?1601 琛?|
| `css/components.css` | 鏂板 `.crossfade-indicator` 鏍峰紡 | 鏂板 |

